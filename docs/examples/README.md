# Ejemplos

- **`sample_runner_output.json`** — forma esperada del JSON que imprime `compound_optimize_runner.py` (con `walk_forward` si usás `--holdout-frac > 0`). Sirve para contrastar pipelines y tests sin pegar corridas reales gigantes.
